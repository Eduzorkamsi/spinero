// import React from "react";

// const activeIcons = ({ Icons = ["SearchIcon", "Heart", "CartIcon", "FamilyIcon"] }) => {
//     const [activeIcon, setIcon] = useState(null);

//     return (
//         <>
//             {Icons.map(Icon => <p key={Icon} onClick={() => { setIcon(Icon) }} style={{ color: activeIcon=== Icon ? "red" : "green" }}>{Icon}</p>)}
//         </>
//     );
// };

// export default activeIcons